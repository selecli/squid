#ifndef BASE64_URLENCODE
#define BASE64_URLENCODE

#include <stdlib.h>
#include <stdio.h>

extern unsigned char* urlencode(unsigned char*, int);
extern int urldecode(unsigned char*, int);
extern unsigned char* base64_encode(const unsigned char*, size_t);
extern unsigned char* base64_decode(const unsigned char*, size_t);

#endif
